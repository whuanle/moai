# 代码熟悉轮次台账（SDD/TDD/BDD/SOD 闭环）

目标：对 moai 代码库逐模块走「熟悉代码 → 生成 SDD/TDD/BDD/SOP 四件套 → 自检闭环」循环，至少 20 轮。每轮自检必须用真实证据（命令输出/测试结果/HTTP 实测）确认文档与代码一致，才算闭环。

规则：
- 后端模块文档放 `docs/<模块>/`，前端模块文档放 `ui/docs/<模块>/`，每轮四件：sdd.md / tdd.md / bdd.md / sop.md。
- 每轮的 tdd.md 末尾附「自检记录」：跑过的命令与结果。
- 台账状态：✅ 闭环 / 🔄 进行中 / ⬜ 未开始。

| 轮 | 模块 | 文档目录 | 状态 | 自检证据 |
|---|---|---|---|---|
| 1 | 用户管理（后端+前端） | docs/user-management/ | ✅ | E2E 34/34、vitest 42/42、lint/tsc 绿（2026-09-01） |
| 2 | auth 领域（后端） | docs/auth/ | ✅ | 锁定自检 8/8（local-dev/auth-lockout-check.mjs）；E2E 34/34；发现缺陷：redirectUrl 校验死代码（已记录） |
| 3 | account 领域（后端自助） | docs/account/ | ✅ | 终审实测（audit-345.mjs）：userinfo/update 缓存失效/unbind 404/bound_accounts 全过 |
| 4 | settings 领域（后端） | docs/settings/ | ✅ | 终审实测：GET/PUT 生效、非法 key 400 |
| 5 | oauthconnect 领域（后端） | docs/oauthconnect/ | ✅ | 终审实测：create/list/软删除/重名拒绝；**PUT 缺陷已修复**（实测 200，文档已同步） |
| 6 | common 领域（后端） | docs/common/ | ✅ | 实测 serverinfo 五字段/publicStoreUrl=/static；build_guid 匿名 401（修正初稿"匿名"说法）、带 token 200 v7；补强：RSA 密钥机制（configs/rsa_private.key 首启生成 2048 PKCS8、一钥三用），node 派生公钥与 serverinfo rsaPublic 逐字节一致（2026-09-02） |
| 7 | storage 领域（后端） | docs/storage/ | ✅ | 终审全链路实测（audit-storage.mjs，跑两次均 7/7）：预上传→PUT 预签名直传 MinIO→完成→匿名 /static 访问字节一致；秒传复用 fileId；fileSize=0 400；私有前缀 404 |
| 8 | hangfire 领域（后端） | docs/hangfire/ | ✅ | Redis 实证：hangfireservers 1 实例、recurring-job:counter 定义/执行链 LastJobId 一致；发现缺陷：counter cron 为 `* * * * ? *`（每秒语义，被 10s 轮询钳制约 10s 一次），源码注释"每分钟"是错的（2026-09-02） |
| 9 | infra 基础设施（后端） | docs/infra/ | ✅ | dotnet build 0 错；MAI_FILE 覆盖链路实测（serverinfo serviceUrl=5210 而非 configs 的 5000）；SystemOptions 全字段对照源码（2026-09-02） |
| 10 | database + PostgresScaffold（后端） | docs/database-scaffold/ | ✅ | psql 实证：6 表/vector 0.8.6/唯一复合索引/种子 admin+root+classify 99 行；序列 setval last_value=max+1；Redis moai:* 键命中；工具构建 0 错（2026-09-02） |
| 11 | 前端认证流（登录/注册/OAuth/token 续期） | ui/docs/auth-flow/ | ✅ | jwt 宽限 60s 核对；/login、/oauth_login 均 200；E2E 34/34 覆盖 RSA 链路；前端三件套绿 |
| 12 | 前端 Kiota API 层 | ui/docs/api-layer/ | ✅ | typecheck 0 错；kiota=1.27.0；usermanage 路径齐全；Feedback 测试 15/15 |
| 13 | 前端主题系统（design-system/theme） | ui/docs/theme/ | ✅ | theme 单测 6/6；主色实测 #2970FF（旧文档 #4A9EFF 已过时并记录）；index.css 无 CSS 变量确认 |
| 14 | 前端基础组件（Page/Card/DataTable/QueryBar/PageToolbar） | ui/docs/components-base/ | ✅ | 定向 vitest 5 文件 11 用例全过（Page2/StatCard1/DataTable4/QueryBar2/PageToolbar2）；全量 42/42 + lint 0 警告；props 逐项对照源码，i18n 键实测取值（2026-09-02） |
| 15 | 前端表单与反馈（FormPage/DetailPage/Feedback/Chat） | ui/docs/components-form/ | ✅ | FormPage/DetailPage/Chat 7/7 + Feedback 15/15 单测通过；导出口径比对一致 |
| 16 | 前端布局导航与路由 | ui/docs/layout-routing/ | ✅ | 路由表 11 条逐一比对；6 个 SPA 路由 200；未实现菜单项兜底行为确认并记录 |
| 17 | 前端状态管理与 i18n | ui/docs/store-i18n/ | ✅ | store 三路 persist 键名核对（moai-web-store/theme/locale）；i18next 12 业务前缀清点；vitest 42/42 + tsc 0 错（2026-09-02） |
| 18 | 前端账号设置页 AccountSettings | ui/docs/page-account/ | ✅ | 头像直传管线（SHA-256→pre_upload→PUT→complate→/account/avatar）逐环节对照源码；两处历史样式偏差如实记录（2026-09-02） |
| 19 | 前端 Settings/OauthConnect 管理页 | ui/docs/page-admin/ | ✅ | dirty 跟踪/回滚与渠道 CRUD 对照源码；发现后端存量缺陷：渠道编辑 PUT 固定 400（SharpGrip），已记录规避法；**终审更新 2026-09-02：该缺陷已修复**（Validate 移除路由回填字段规则，audit-345 实测 PUT 200） |
| 20 | 前端 Dashboard 与测试基建 | ui/docs/dashboard-testing/ | ✅ | Dashboard 静态假数据/占位路由行为确认并记录；13 测试文件 42 用例分布清点；新页面写测试五步模板（2026-09-02） |
| 21 | 部署与本地环境（Docker/entrypoint/local-dev） | docs/deployment/ | ✅ | docker compose config -q 通过；serverinfo 200（serviceUrl=5210 证实 MAI_FILE 覆盖）；发现阻断缺陷 D1/D2 并推动修复；**2026-09-03 容器全链路验收 ✅**：镜像构建（arm64）+ serverinfo 200 + e2e 41/41（UM 34 + 存储 7 容器内跑）；D3 修复（entrypoint S3 段）；移除 apt 死重；新增 D6/Apple Silicon 注意事项 |
| 22 | 文档体系重构（分层+Gherkin+互链） | docs/DOC-STANDARD.md + 全部 21 模块 | ✅ | doc-audit.py 全过：385 场景全部编号化（@缩写-Sn+@auto/@manual）、四件互链 21×4、篇幅达标、链接/锚点零悬空；回归 63/63 E2E + 42/42 vitest + 15/15 浏览器点击（2026-09-02） |
| 23 | 团队模块（数据库+API+前端） | docs/team/ | ✅ | team/team_user 建表（bool 软删除+partial 唯一索引，psql 冒烟）；team-e2e.mjs **35/35**；前端 Teams 页 3 测试 + vitest 45/45 + tsc/eslint 绿；全量回归 UM34+ACC/SET/OC14+STO7+AUTH8+BDD36+deep68 全过；浏览器走查建团/成员/解散（2026-09-02）；另修复台账 P4（Dockerfile ui/moai→ui + net10 镜像 + MAI_FILE）与 P5（OTLP 空配置容错） |
| 24 | 团队模式二期（转让所有权+团队头像） | docs/team/ | ✅ | 新端点 PUT owner / POST avatar；team-e2e.mjs 47/47（TM-13 转让角色互换、TM-14 头像存储全链路+防伪造）；前端设置弹窗+转让入口，vitest 46/46；浏览器走查通过；全量回归 UM34+14+7+8+BDD36+deep68 全过（2026-09-02） |
| 25 | 知识库模块（数据库+API+前端）+ 团队模式上下文 | docs/wiki/ | ✅ | wiki 建表（bool 软删除+partial (team_id,name) 唯一）；wiki-e2e.mjs **23/23**；前端 store.currentTeamId+侧边栏切换器+/wiki 页（4 测试），vitest **50/50**；全量回归 UM34+14+7+8+team47+BDD36+deep68 全过；浏览器走查通过（2026-09-02）；另解决并行 stash-pop 冲突（aichannel vs team 三方保留） |
| 25 | 知识库模块（数据库+API+前端）+ 团队模式上下文 | docs/wiki/ | ✅ | wiki 建表（bool 软删除+partial (team_id,name) 唯一）；wiki-e2e.mjs **23/23**；前端 store.currentTeamId+侧边栏切换器+/wiki 页（4 测试），vitest **50/50**；全量回归 UM34+14+7+8+team47+BDD36+deep68 全过；浏览器走查通过（2026-09-02）；另解决并行 stash-pop 冲突（aichannel vs team 三方保留） |
| 26 | 知识库文档层（内容协作） | docs/wiki/ | ✅ | wiki_document 建表（text 正文）；文档 5 端点（Member 可增改、Admin+ 删）；wiki-doc-e2e.mjs **15/15**；前端 /wiki/:id 文档页+编辑器（2 测试），vitest **52/52**；全量回归 252 项后端断言全过；浏览器走查建文档/编辑器回显（2026-09-03） |
| 27 | 变量管理模块（数据库+API+前端） | docs/variable/ | ✅ | team_variable 建表（私密值 AES 加密+partial (team_id,key) 唯一）；variable-e2e.mjs **26/26**（私密值全程未泄露）；前端 /variable 页+侧边栏入口（4 测试），vitest **56/56**；IVariableService 供插件运行时 ${key} 服务端替换；全量回归全过；浏览器走查+截图（2026-09-03） |
| 28 | 变量字段修正：分组→变量名称 + 私密值永不回传 + key 可改 | docs/variable/ | ✅ | team_variable 列 group_name→name（变量名称）；后端 Shared/Core/Api 全链替换；私密变量值详情/列表恒 null（永不回传前端），编辑留空=保持、填写=覆盖；key 可修改（团队内唯一兜底 409）；前端 /variable 页改名称筛选/列、编辑可改 key、值列掩码；变量三件套 0 错误；前端类型检查+lint+vitest **72/72**（21 文件）全绿（2026-09-04） |
| 29 | 团队插件模块（系统插件私有授权 + 团队自定义/动态插件） | docs/teamplugin/ | 🔄 | 新增 plugin_team_authorization 表（DDL 脚本）；后端 src/teamplugin 三件套 + aiplugin 授权端点；前端 /plugin 授权抽屉 + 团队详情「插件」区块；构建 0 错误（teamplugin.Core/database.Postgres/aiplugin.Custom）；前端 vitest **99/99** + tsc + eslint 全绿；team-plugin-e2e 待执行（后端需运行 5210 并建表）（2026-09-07） |
| 30 | 知识库：embedding 设置粒度修正（wiki 只绑向量模型，按文档动态配置元数据/切片） | docs/wiki/ | ✅ | 删 wiki 表 metadata_model_id / chunk_size / chunk_overlap 三列（DDL wiki_vectorization.sql 收紧到删除列 + embedding_dimensions 上限 2000 CHECK）；UpdateWikiEmbeddingCommand 仅留 EmbeddingModelId + EmbeddingDimensions（维度上限 2000）；EmbeddingDocumentCommand/TaskMessage/Handler/Consumer 增加 metadataModelId/chunkSize/chunkOverlap；QueryWikiDocumentEmbedding 切片参数从 `document.slice_config` JSON 解析；前端 `WikiDetail` 设置页只保留 embedding 模型 + 维度，`WikiDocumentDetail` 改为文档级向量化参数表单（chunkSize/Overlap/metadataModel/embedSource/embedMetadata）；i18n 同步；WikiDetail.test.tsx 重写为 18 用例，前端 typecheck/lint 0 警告、vitest **116/116**；E2E wiki-e2e.mjs 待后端可运行实测，dotnet build 待系统终端复验（沙箱 NuGet ConfigurationDefaults 阻） |
| 31 | 知识库：可选重排序模型设置（锁定后仍可修改） | docs/wiki/ | ✅ | wiki 新增可空 rerank_model_id（DDL asserts/wiki_rerank_model.sql）；模型类型新增 `rerank`（AIModelKind.Rerank + AIModelMetaMapper 推导，须先于 embedding 判定）；新增 UpdateWikiRerankModelCommand/Handler + `PUT /api/wiki/{id}/rerank-model`（Admin+，**不校验 IsLock**，传 null 解绑，仍校验模型启用/类型/团队授权）；QueryWiki 返回 rerankModelId/Name，model-options 增加 rerankModels；前端设置页新增「重排序模型」卡片（allowClear 可选、锁定态仍可编辑），i18n zh/en 同步；后端 `dotnet build MoAI.Wiki.Api --no-restore` 0 错误（MoAI 宿主因进程占用 dll 未复验），前端 typecheck/lint 0 error、vitest **129/129**；顺带修复既存 flaky 用例（文档内容模态「关闭」按钮改为等待式断言，antd 两字按钮会插入空格）；DDL 需在有库环境手动执行，Kiota 客户端需重启后端后 `npm run syncapi` |
| 32 | AI 渠道模型：重排序类型补齐（前端可选 + 同步识别规则） | —（aichannel 暂无四件套，口径见源码） | ✅ | `AIModelMetaMapper` 新增 `IsRerankModel`/`ResolveModelKind`：模型 id/名称/模型族 任一含 `rerank`（覆盖 `Reranker` 写法）即判为 `rerank`，优先于目录声明类型，避免同步后被判成文本模型；`SyncAIModelCommandHandler` 新增/存量两条路径统一走 `ResolveModelKind`；前端 `Models.tsx` 模型类型下拉与标签新增「重排序」（`models.kindRerank`，zh/en 同步），重排序类型隐藏能力开关与上下文/最大输出；后端 `dotnet build MoAI.AIChannel.Core --no-restore` 0 错误，前端 tsc/eslint 0 错误（vitest 见本轮自检） |
| 33 | 系统设置：Neo4j 知识图谱设置（仅设置 + 配置能力暴露），并移除 oauth_auto_register | docs/settings/ | ✅ | 内置 4 项 `OPEN_NEO4J`/`NEO4J_URI`/`NEO4J_USERNAME`/`NEO4J_PASSWORD`（种子自动生成，默认 false/空）；`IKnowledgeGraphSettingsService` 读取出口（未开启短路 `Enabled=false`）；前端 `Settings.tsx` 知识图谱卡片，开关开启后方可填连接信息，关闭保存不提交连接项；`oauth_auto_register` 无任何业务代码消费，连同 @SET-S10 一并移除，系统设置页与侧边栏入口改为 root 专属（非 root 重定向）；zh/en i18n 同步；后端 `dotnet build MoAI.Settings.Core` 0 错误（MoAI 宿主因运行进程占用 dll 未复验），前端 typecheck/lint 0 错误、`Settings.test.tsx` 4/4；@SET-S16~S19 写入四件套（2026-09-10） |
| 34 | 团队插件：自定义/动态两 Tab + 对齐管理员能力（详情/函数/刷新/预上传/运行/动态模板） | docs/teamplugin/ | ✅ | 后端新增 6 端点；修复动态实例创建未关联导致列表缺失；实例 key 跨团队冲突 409；team-plugin-e2e.mjs **19/19**（TP_BASE=:5000）；前端 TeamPlugins 拆分两 Tab 并复用管理员弹窗（可注入 loadDetail/loadFunctions/uploadFile/runPlugin），新增 TeamPlugins.test 3 用例；MoAI 宿主 dotnet build 0 错误；vitest **192/192** + tsc + eslint 全绿（2026-09-10） |
| 35 | 团队模块：管理员团队治理（查看全部团队 / 禁用启用 / 转让负责人） | docs/team/ | 🔄 | 新增 `/api/admin/team/list`、`/api/admin/team/{id}/disable`、`/api/admin/team/{id}/owner`（归 team 模块；Controller 层 EnsureAdmin 门禁，Handler 只做事实校验）；`TeamEntity.IsDisable` 首次获得写入口（D6：仅状态标记，不做跨模块级联）；转让目标扩大为系统内任意用户（非成员自动入团为 Owner，原 Owner 降 Admin）；前端新增 `/admin/teams` 页 + 侧边栏管理员菜单「团队管理」（AppSider adminNav，ApartmentOutlined），i18n zh/en 同步；team-e2e.mjs 补 TM-15/16 共 21 断言（预期 68/68）；前端 AdminTeams.test **3/3 PASS** + eslint 0 error + tsc 仅 3 处待 `npm run syncapi` 的 `client.api.admin`；**后端 dotnet build 与 E2E 待系统终端复验**（本机 NuGet ConfigurationDefaults 阻断，同轮次 30/31 记录；`MoAI.Team.Core.csproj` 新增 `MoAI.Account.Shared` 引用，需先 `dotnet restore`）（2026-09-10） |
| 36 | 团队应用：应用管理（Agent/流程应用的创建与基础信息） | docs/app/ | 🔄 | 新增 `src/app` 三层 + `/api/app` 五端点（创建/列表/详情/更新基础信息/头像）；`AppType` 枚举（Agent=0/流程=1）并修正 `app.app_type`/`AppEntity`/`AppConfiguration` 旧注释；权限按团队角色——Owner/Admin 创建与管理、Member 只读、非成员 404；`asserts/app.sql` DDL（含 `idx_app_name_index`/`idx_app_team_id_index`）；前端激活侧边栏死菜单 `/app`（`nav.app`）：表格聚合我的团队应用 + 新建/编辑弹窗 + 头像直传 + i18n zh/en；`local-dev/app-e2e.mjs` **28 断言**（待执行）；前端全量 vitest **221/221（39 文件，含 Apps.test 3 用例）** + eslint 0 error，`tsc` 仅待 `npm run syncapi` 的 `client.api.app` 5 处（另存轮次 35 遗留 `team.ts` 3 处）；**后端 dotnet build 与 E2E 待系统终端复验**（本机 NuGet `ConfigurationDefaults` 阻断，既有项目同样失败，同轮次 30/31/35）；docs/app 四件套（AP-S1~S10）（2026-09-10） |
| 37 | 团队应用：Agent 应用配置与会话表（仅 DB 层） | docs/app/ | 🔄 | 三表落 `asserts/app_agent.sql`：`app_agent_config`（提示词/模型/知识库/插件/执行参数，与 `app` 1:1 partial 唯一）/ `app_agent_session`（会话列表，`last_message_time` 倒序 + `user_type` 预留外部用户）/ `app_agent_message`（对话历史，`seq` 排序 + 唯一 `(session_id, seq)`）；**取代 legacy `app_chatapp*` 三表**（废弃不读写）；`src/database` 新增 3 实体 + 3 Configuration（`ApplyConfigurationsFromAssembly` 自动扫描）+ `DatabaseContext` 3 DbSet；**关键修正：legacy `model_id integer` → 改 `uuid`**（对齐 `AiModelEntity.Id`，`wiki.id` 为 int、`plugin.id` 为 Guid、`app.id` 为 uuid）；`wiki_ids`/`plugins` 用 JSON 数组文本、`execution_settings` 用 JSON 对象文本（legacy `'[]'` 为误用），绑定团队可访问性校验放应用层、不建物理外键；`docs/app/sdd.md` 增 §2.1 与决策 D8~D13；**无 API/前端**；`dotnet build src/database/MoAI.Database.Postgres --no-restore` → **0 error、新增文件 0 告警**（宿主因 `src/knowledgegraph` 的 `KnowledgeGraphEntity`→`KgEntity` 改名残留 CS0246×4 未整体通过，非本轮引入）；顺带修复脚手架模板带来的 CS0763 与 `Wiki1Entity` CS0535（详见当日记忆）（2026-09-10） |
| 38 | 团队应用：入口修正（应用管理移入团队页「应用」分区，成员只能使用） | docs/app/ | ✅ | 移除侧边栏一级「应用」（`nav.app`）、`/app` 路由与 `ui/src/pages/apps` 页面——跨团队聚合页把「管理」放到了团队之外，方向错误；新增 `ui/src/pages/teams/apps/TeamApps.tsx` 挂在团队页 `SectionKey='apps'`（`/team/:id/apps`，与 TeamWikis/TeamPlugins 同构），按 teamId 单团队查询、新建弹窗去掉团队选择、`canManage` 控制新建/编辑；`TeamManage` 按角色收敛分区——Member 只保留 信息/应用/知识库，成员/网关/插件/变量/设置 隐藏且 URL 直达回落「信息」；前端仅渲染层收敛，后端 Handler 门禁不变（Member 403 / 非成员 404）；i18n 增 `team.apps`、`appManage.memberHint`，删 `nav.app` 与 `appManage.{team,teamPlaceholder,colTeam,noManageTeam}`；修复 `TeamManage.test.tsx` 的 mock 跨用例泄漏（`mockResolvedValue` 改为 beforeEach 复位）并补成员可见性 2 用例；`TeamApps.test` 3/3 + `TeamManage.test` 14/14，前端全量 vitest **223/223（39 文件）** + `eslint src` 0 error，`tsc -b` 仅待 `npm run syncapi` 的 8 处（`client.api.app` 5 + 轮次 35 遗留 `team.ts` 的 `client.api.admin` 3）；docs/app 四件套同步（D14、@AP-S11/S12）；**后端无改动，dotnet build 与 app-e2e 仍待系统终端复验**（沙箱 NuGet `ConfigurationDefaults` 阻断，同轮次 30/31/35/36/37）（2026-09-11） |
| 39 | 团队应用：新建/编辑支持应用头像与「允许外部使用」开关 | docs/app/ | ✅ | 创建命令增 `Avatar`(objectKey) 与 `EnableForeign`、更新命令增 `EnableForeign`，`AppItem`/`QueryAppCommandResponse` 增 `EnableForeign`；**创建时头像走「先直传拿 objectKey → 随创建请求提交」**（应用尚不存在、无法调头像接口），objectKey 仍校验 `file.is_uploaded`（伪造 404，E2E AP-14）；外部开关落 **`app.enable_foreign`**（列注释「允许外部使用」，决策 D16），`is_public` 本期不用；前端新建/编辑弹窗各加头像上传与开关、列表新增「外部使用」列，i18n zh/en 同步；**本轮首次在本机跑通后端并执行 `npm run syncapi`**，`tsc -b` 由 8 处报错（`client.api.app` 5 + 轮次 35 `client.api.admin` 3）**清零**；E2E 由 28 扩至 **35 断言全 PASS**（顺带修 AP-08a `teamId` 字符串/数字比较的历史 bug）；`dotnet build src/MoAI` 0 error、前端 vitest **226/226（39 文件）**、`eslint .` 0 error（2026-09-11） |
| 40 | 团队应用：卡片列表 + 应用管理页（Agent 应用配置：插件/知识库/提示词） | docs/app/ | ✅ | 前端应用列表由 `DataTable` 改**卡片网格**，卡片右上角「管理」进独立管理页 `/team/:teamId/app/:appId/:section?`（`info`/`plugins`/`knowledge`/`prompt`，布局同 `WikiDetail`；流程应用只留 基础信息 并提示未开放）；原编辑弹窗并入管理页「基础信息」分区；后端起 `GET|PUT /api/app/{id}/agent-config`（`QueryAppAgentConfigCommand`/`SaveAppAgentConfigCommand` + 两个 Handler，新增 `AppAgentConfigJson` 收口 JSON 列读写）：**成员可读、Admin+ 可写、非成员 404**；`wikiIds` 校验 `wiki.team_id == app.team_id`、`plugins` 校验「团队自有 or 系统公开/已授权本团队」，**越权返回 400 且校验先于写入**（E2E AP-17d），提示词 ≤ 4000；**仅 Agent 应用可配**（流程应用 400），未保存过配置返回空配置而非 404；前端管理页选项直接取 `team/plugin/list` 与 `wiki/list`，取值范围天然等于「该团队有权使用」，空 Guid 内存静态插件展示层过滤；`model_id`/`execution_settings` 无入口（占位落库），下阶段做会话运行。`dotnet build src/MoAI` 0 error；**E2E 由 35 扩至 50 断言全 PASS**（新增 AP-15~AP-19）；前端 `npm run syncapi`（需 `CODEBUDDY_SAFE_DELETE_ENABLED=0`）后 `tsc -b` 0 error、vitest **232/232（40 文件，新增 `AppManage.test.tsx` 6 例、`TeamApps.test.tsx` 改卡片断言）**、`eslint .` 0 error；docs/app 四件套同步（D17~D20、@AP-S15~S19）（2026-09-11） |
| 41 | 动态插件：博查全网搜索内置模板（`bocha_web_search`）+ 补齐动态插件 E2E | docs/aiplugin-dynamic/ | ✅ | 新增 `BoChaWebSearchPlugin`（`IDynamicPluginRuntime` 三件套：配置 `ApiKey` / 请求 `Query·Freshness·Summary·Count·Include·Exclude` / 裁剪响应 `WebPages[]+Images[]`）；外部调用**复用 infra** `IBoChaClient`（`MoAI.AIPlugin.Dynamic` → `MoAI.Infra.ExternalHttp` ProjectReference，构造注入，不 `new HttpClient`）；Refit 非 2xx 的 `ApiException` 转 `BusinessException((int)StatusCode, "HTTP xxx + 响应体")`，`InitAsync` 校验 API Key、`Count` clamp 1-50、空 `Query` 400；顺带修 infra：`WebSearchRequest.Include/Exclude` 改可空（`WhenWritingNull` 下不再下发空串）、`HandleApiError` 各分支补 `StatusCode`；**无 API/前端/DB 变更**（模板自动进入动态模板下拉，无需 `npm run syncapi`）、未新增依赖包；bdd.md 由 `placeholder` 补齐 @DYN-S1~S18，并**新建 `local-dev/dynamic-plugin-e2e.mjs`**（文档引用已久但从未创建），运行态 **PASS 30 / FAIL 0 / SKIP 1**（SKIP 为需真实博查 Key 的人工场景）；无效 Key 实测 `插件执行失败: 博查 Web Search 调用失败（HTTP 401）：{"code":"401",...,"message":"Invalid API KEY"}`，证明 DI 注入与对外调用链路打通；**E2E 反查出文档缺陷并使用文档改正**：管理员侧「实例 key 重复 → 409」实为 upsert（同 key 提交即更新，409 分支为死代码，已记入 sdd 已知问题），路由实际为 `/api/ai/plugin/*`（文档漏写 `/api`，已修正）；sdd/sop/tdd/aiplugin-authoring/AGENTS.md 同步；沙箱内 `dotnet restore` 仍被 NuGet `ConfigurationDefaults` 阻断，宿主整体 `dotnet build` 待系统终端复验（2026-09-11） |
| 42 | 团队应用：管理页改单页左右分栏 + 接入对话模型 | docs/app/ | ✅ | **用户反馈纠偏**：上一轮管理页照 `WikiDetail` 做「左菜单 + 四分区」是错的——配置项只有四项且互相有关联，分区切换反而要来回跳。本轮**去掉左侧菜单**，改为一页左右两栏：**左栏「应用信息」**（头像/类型/名称/描述/允许外部使用 + 「保存信息」）、**右栏「Agent 配置」**（**对话模型**/提示词/插件/知识库 + 「保存配置」）；路由去掉 `:section?`，简化为 `/team/:teamId/app/:appId`（决策 D21）。新增**对话模型**：`SaveAppAgentConfigCommand` 增 `Guid? ModelId`，Handler 新增 `ValidateModelIdAsync` —— 复用团队「模型网关」口径（模型与渠道均启用，且公开或 `ai_model_authorization` 已授权本团队），越权/不可用一律 **400**，未选（null/空 Guid）合法并落空 Guid；Controller 透传，`QueryAppAgentConfigCommandResponse.ModelId` 原有字段直接可用（决策 D22）。前端 `api/app.ts` 的 `saveAppAgentConfig` 增 `modelId`（注意 kiota 这次把 `plugins` 生成为 `Guid[]` 而非 `string[]`，需 `as Guid[]`），选项取自 `getTeamGatewayModels`；`AppManage.tsx` 重写为 `Row`/`Col` 分栏（`xs=24 / lg=10+14`），配置四项共用一份状态与一个保存按钮。i18n zh/en 增 `backToList`/`agentConfigTitle`/`model`/`modelHint`/`modelPlaceholder`/`modelEmpty`/`saveInfo`/`saveConfig`，删无用的 `save`，`sectionInfo` 文案由「基础信息」改为「应用信息」。`AppManage.test.tsx` 重写为单页断言（含模型回显、越权过滤、Member 只读）。`dotnet build src/MoAI` 0 error、改动文件 0 告警；**E2E 由 50 扩至 55 断言全 PASS**（AP-20a~e：模型列表与授权一致、绑定可用模型 200 并回读一致、无权模型 400、失败不写入；为造出可用取值，脚本用 root 管理员临时把本地库首个私有模型授权给 E2E 团队，拿不到 admin 账号则自动 SKIP）；前端 `npm run syncapi`（`CODEBUDDY_SAFE_DELETE_ENABLED=0`）→ `tsc -b` 0 error、vitest **232/232（40 文件）**、`eslint .` 0 error；docs/app 四件套 + README 同步（2026-09-11） |
| 43 | 动态插件：博查 AI 搜索内置模板（`bocha_ai_search`）+ 桩服务端到端验证 | docs/aiplugin-dynamic/ | ✅ | 新增 `BoChaAiSearchPlugin`（配置 `ApiKey` / 请求 `Query`(必填)·`Freshness`·`Include`·`Count`·`Answer` / 响应 `Query`·`ConversationId`·`Answer`·`FollowUps[]`·`WebPages[]`·`Images[]`·`ModelCards[]`·`SomeResultsRemoved`）；复用 infra `IBoChaClient`（`/v1/ai-search`）+ 抽共用 `BoChaAuthorization.Build`（顺带消除全网搜索里的重复实现）；**固定非流式**（插件引擎 `RunAsync` 返回单结果，无法透出 SSE），`messages` 按 `content_type` 重组：`answer`→`Answer`(Markdown)、`follow_up`→`FollowUps`、`webpage`/`image`→逐条展开（兼容非流式 `{"value":[...]}` 包装与流式裸对象/裸数组）、其余 `content_type`→`ModelCards`（`Type` 即 `content_type`，通用字段 `name/url/snippet/summary/siteName/siteIcon/datePublished`，缺 `snippet` 回落 `description`，通用字段全空则丢弃），空 `video`（`{}`）不产出条目；参数兜底 `Count` clamp 1-50 / `Freshness` 空白回落 `noLimit` / 空 `Query` 400 / `Answer`·`Include` 透传；上游错误与全网搜索一致归一为带响应体的 `BusinessException`；**顺带改 infra**：`InfraExternalHttpModule` 的博查 `BaseAddress` 支持 `MoAI:BoCha:Endpoint` 覆盖（默认官方地址不变），使成功路径可被「无 Key」自动化验证；**无 API/前端/DB 变更**、未新增依赖包；**新增 `local-dev/bocha-search-e2e.mjs`**（自带 BoCha 桩服务 + 拉起指向桩的独立后端 :5199）：**PASS 22 / FAIL 0**，首次把 @DYN-S16 成功路径从「人工待验证」转为自动覆盖；`dynamic-plugin-e2e.mjs` 扩至 **PASS 37 / FAIL 0 / SKIP 2**（新增 @DYN-S19~S21）；真实上游实测（非桩）无效 Key 返回 `插件执行失败: 博查 AI Search 调用失败（HTTP 401）：{"code":"401","log_id":"10bfe899d9ef3257","message":"Invalid API KEY"}`；bdd 增 @DYN-S19~S22、sdd/sop/tdd/aiplugin-authoring 同步，tdd 更正「`-t:CoreCompile` 可作编译证据」的旧说法；沙箱内 `dotnet restore` 仍被 NuGet `ConfigurationDefaults` 阻断，宿主整体 `dotnet build` 待系统终端复验（2026-09-11） |
| 44 | 动态插件：飞书自定义机器人推送文本（`feishu_webhook_text`） | docs/aiplugin-dynamic/ | ✅ | 新增 `FeishuWebhookTextPlugin`（`IDynamicPluginRuntime` 三件套：配置 `WebhookKey`/`SignKey` / 请求 `Text` / 响应 `Code`/`Msg`/`Text`），复用 infra `IFeishuWebHookClient`（Refit + `ExternalHttpMessageHandler`，路由模板 `bot/v2/hook/{key}`）；**WebHook 输入归一化**：`NormalizeWebhookKey` 从完整地址里截取最后一个 `/hook/` 之后的 token，并切掉 `?`/`#`/尾随 `/`，避免用户粘贴查询串/路径斜杠导致飞书返回 `19001`；签名校验开关时由 `BuildSign` 补 `timestamp + sign`；**错误归一**：Refit 非 2xx 的 `ApiException` 转 `BusinessException((int)StatusCode, "飞书推送失败（HTTP xxx）：{body}")`；HTTP 200 但 `code != 0` 按飞书文档码表映射——`19001`/`19007`/`19021`/`19022`/`19024` 全部 400，`9499` 429，其余 500；参数兜底 `WebhookKey` 在 `InitAsync` 校验（实例进入运行抽屉前即被拒）、`Text` 在 `RunAsync` 校验为空即 400；**无 API/前端/DB 变更**、未新增依赖包（沿用既有 Refit 15.2.0）；顺手用源生成器产物核验 Refit 生成的 `{key}` 路径参数确实拼到 URL（实测字符串 `bot/v2/hook/11111111-2222-3333-4444-555555555555`、无 `?key=` 查询串），infra 无需改；本轮首次在沙箱内**完整跑通后端并执行 `node local-dev/dynamic-plugin-e2e.mjs`**，脚本由 37 扩至 **PASS 44 / FAIL 0 / SKIP 2**（新增 @DYN-S23 注册表含 `feishu_webhook_text` + @DYN-S24 空 WebhookKey / 空 Text / 占位 token → 飞书返回 `19001`），真实飞书上游实测 `插件执行失败: 飞书推送失败（19001）：Webhook 地址无效，请核对机器人 Webhook 配置`；bdd 增 @DYN-S23/S24、sdd 内置模板表 + `feishu_webhook_text` 细节段、sop 飞书排障小节 + 内置模板清单扩展；tdd 新增两条 PASS 链接（2026-09-11） |
| 45 | 动态插件：PaddleOCR 三内置模板（PP-OCRv5 / PP-StructureV3 / PaddleOCR-VL） | docs/aiplugin-dynamic/ | 🔄 | 把旧 `MoAI.Plugin.NativePlugin.Paddleocr/*`（三个 `[NativePluginConfig]` 插件）按最新插件机制重写到 `src/aiplugin/MoAI.AIPlugin.Dynamic/`；新增 7 Model（`PaddleOcrConfig` 共享 `ApiUrl`/`Token`、三个 `Request`/`Response`/`Page`）+ 1 个共享辅助 `PaddleOcrAuthorization.Build`（拼 PaddleOCR 协议要求的 `Authorization: token {Token}`，空 Token 短路返回空串）+ 3 个 `IDynamicPluginRuntime` 实现：`PaddleOcrPlugin`（`POST /ocr`，PP-OCRv5）、`PaddleStructureV3Plugin`（`POST /layout-parsing` 形态 StructureV3）、`PaddleVlPlugin`（`POST /layout-parsing` 形态 VL，强制 `Visualize=true`）；**复用 infra** `MoAI.Infra.Paddleocr.IPaddleocrClient`（构造注入，**不 `new HttpClient`**）；`BaseAddress` 每次执行按实例 `ApiUrl` 重设（`AddRefitClient` 注册为 transient，HttpClient 独立、并发运行互不干扰）；**命名空间消歧**：`PaddleOcrResponse<T>` 在 infra 同时存在 `MoAI.Infra.Paddleocr`（Refit 客户端用）与 `MoAI.Infra.Paddleocr.Models`（业务模型用）两个命名空间；插件用 `using InfraPaddle = MoAI.Infra.Paddleocr;` + `using InfraPaddleModels = MoAI.Infra.Paddleocr.Models;` 两个别名，与自身的 `PaddleOcrRequest`/`PaddleOcrResponse` 区分；响应按页聚合 `Pages[]`，OCR 的 `Text` 由 `rec_texts` 按行拼接；StructureV3 沿用旧版「`seal_res_list[].rec_texts[0]`」语义但改为按印章拆为 `SealTexts[]`，`prunedResult` 一律 `JsonElement.GetRawText()` 回写为 `PrunedResultJson`；VL 直接吐 `MarkdownText`/`MarkdownImages`；**无 API/前端/DB 变更**（模板自动进入动态模板下拉），未新增依赖包；`dotnet build src/aiplugin/MoAI.AIPlugin.Dynamic --no-restore -p:GenerateDependencyFile=false` → **0 error、0 告警**；**新增 `local-dev/paddleocr-e2e.mjs`**（桩服务监听 `/ocr` + `/layout-parsing`，对未带 `token ` 的 Authorization 一律返 401，VL/StructureV3 由请求体特征字段 `useLayoutDetection`/`useSealRecognition` 区分），覆盖 @DYN-S36~S42；bdd 增 @DYN-S36~S42、sdd 内置模板表新增 `paddleocr_*` 三行 + 共享细节段（`paddleocr_*` 1~7） + 末尾 E2E 提示、tdd 表新增七条「待运行」、构建与回归段补 `node local-dev/paddleocr-e2e.mjs`、自检记录加本轮；AGENTS.md 验证命令增 `paddleocr-e2e.mjs` 行；沙箱内 `dotnet restore` 仍被 NuGet `ConfigurationDefaults` 阻断，宿主整体 `dotnet build` 与桩服务 E2E 仍待系统终端复验（2026-09-11） |
| 46 | 静态插件：旧 `MoAI.Plugin.Tool` 五个工具插件迁移到 `MoAI.AIPlugin.Static` | docs/aiplugin-static/ | 🔄 | 按最新静态机制重写 `CurrentTimePlugin`/`FlowWaitPlugin`/`MarkdownToHtmlPlugin`/`TextExtractPlugin`/`WebContentFetchPlugin`，key 统一 `static_` 前缀（`static_current_time`/`static_flow_wait`/`static_markdown_to_html`/`static_text_extract`/`static_web_content_fetch`），强类型请求/响应模型（`[Description]` 标注）；旧 `KmTextExtractionHandler.ExtractUrlAsync` 改为 `Maomi.ToMarkdown.TextExtractionService.ExtractAsync(stream, fileName)`（按后缀选抽取器），文件下载统一走 infra `IPutClient`（构造注入，不 `new HttpClient`）；网页抓取移植 `AngleSharpHelper`，用 `IPutClient` 抓 HTML + AngleSharp 解析，10s 超时/取消归一为 `BusinessException`；`MoAI.AIPlugin.Static.csproj` 加 `Markdig`/`AngleSharp`/`Maomi.ToMarkdown` 包引用与 `MoAI.Infra.ExternalHttp` 项目引用；`Directory.Packages.props` 的 `AngleSharp` 由 `1.4.1-beta.502` 升到 `1.5.2`（对齐 `Maomi.ToMarkdown → ReverseMarkdown` 传递依赖，并消除 NU1902 漏洞告警）；`dotnet build src/MoAI/MoAI.csproj` **0 error**、新文件 0 告警；docs/aiplugin-static 四件套增 @STP-S10~S14 与内置插件清单，aiplugin-authoring 同步；运行抽屉走查与 E2E 待系统终端复验（沙箱同轮次 41/43/45）（2026-09-11） |
| 47 | 动态插件：PostgreSQL / MySQL 只读查询内置模板（`postgres_query` / `mysql_query`） | docs/aiplugin-dynamic/ | ✅ | 参考旧 `MoAI.Plugin.NativePlugin` 的 `PostgresPlugin`/`MysqlPlugin`，按**最新动态插件机制**重写（弃 `[NativePluginConfig]`+`INativePluginRuntime`+SemanticKernel `[KernelFunction]`，改 `IDynamicPluginRuntime<Req,Resp,Config>` + `[AiPlugin(key)]`，请求/配置强类型并加 `[Description]`）；**核心约束「只能只读、不可增删改」用三层防护落地**：① **文本层** `SqlReadOnlyGuard`——先剥离行/块注释、字符串字面量（含 PG 美元引用 `$tag$`）、双引号/反引号标识符，再校验「只允许单条语句 + 首关键字白名单（SELECT/WITH/TABLE/VALUES/SHOW/EXPLAIN/DESCRIBE/DESC）+ 全句关键字黑名单（DML/DDL/权限/写文件/过程与动态执行/会话与事务控制/库维护）」，MySQL `/*! */` **可执行注释内容保留参与扫描**；校验先于建连，故与目标库是否可达无关；② **连接层**服务端强约束——PG 用 `SELECT set_config('default_transaction_read_only','on',false)`、MySQL 用 `SET SESSION TRANSACTION READ ONLY`，即使漏过文本层（典型为以 SELECT 形态调用有副作用函数），服务端也会以 25006/1792 拒绝；③ **资源层**——`MaxRows`(1-1000) 截断 + PG `statement_timeout` / ADO.NET 命令超时(1-300s)；新增 7 文件（`SqlReadOnlyGuard` / `SqlQueryResult` / `SqlResultReader` + 两套 `Config`/`Request`/`Response` + 两个 `Plugin`），`SqlResultReader` 处理同名列（`_2`/`_3`）、`DBNull`→null、`byte[]`→Base64、超 `MaxRows` 多读一行置 `Truncated`；`MoAI.AIPlugin.Dynamic.csproj` 加 `Npgsql`(10.0.3) 与 `MySqlConnector`(2.5.0)（`Directory.Packages.props` 中心化）；`dotnet build src/aiplugin/MoAI.AIPlugin.Dynamic --no-restore -p:GenerateDependencyFile=false` → **0 error、0 告警**（PG 会话设置改参数化后消除 `CA2100`/`CA1863`）；`node local-dev/dynamic-plugin-e2e.mjs` → **PASS 102 / FAIL 0 / SKIP 3**（新增 @DYN-S29~S32 共 34 断言：注册表示例与配置类型、12 类写操作/多语句被拒、6 条合法只读放行到连接层、PG 真实库下 MaxRows 截断 + 会话只读 `on` + 同名列去重 + bytea/jsonb/时间类型 + 空结果集；@DYN-S33 MySQL 因本机与同网段无可达 MySQL、Docker 守护进程未运行而 SKIP，脚本已按 `MYSQL_E2E_CONNECTION` 开关就绪）；**顺带修 @DYN-S27d 实现缺口**（JS 执行器脚本缺 `run` 时 Jint 抛 `ReferenceError` 只剩不可读的 `run is not defined`，改为先 `engine.Evaluate("typeof run === 'function'")` 探测再给可读提示）与**该脚本自身 5 处断言缺陷**（@DYN-S26a/S26b/S28a 请求体在单引号串里写 `\"` 拼出非法 JSON、@DYN-S26b/S26c-string/S26c-array 期望与「`ResultJson` 恒为 JSON 文本」契约相反 → 改用 `JSON.stringify` 构造请求 + `safeParse` 解析后比对）；`bocha-search-e2e.mjs` 回归 **PASS 22 / FAIL 0**；bdd 增 @DYN-S29~S34、sdd 内置模板表 + `postgres_query`/`mysql_query` 三层防护细节段 + JS 细节 #4 更正、tdd 增映射行与自检记录（2026-09-11） |

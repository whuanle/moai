# 应用管理模块操作手册（SOP）

> 关联：[SDD](./sdd.md) ｜ [BDD](./bdd.md) ｜ [TDD](./tdd.md) ｜ [SOP](./sop.md)

## 1. 概念

应用是**团队下的产物**，分两类：

| 类型 | app_type | 说明 | 本期能力 |
|---|---|---|---|
| Agent 应用 | 0（`agent`） | 以模型 + 提示词 + 知识库/插件编排的对话式应用 | 创建 + 基础信息 + **应用配置（对话模型/插件/知识库/提示词）** |
| 流程应用 | 1（`workflow`） | 以流程编排驱动的应用 | 创建 + 基础信息（配置能力未开放） |

应用的创建与配置需要**团队管理员**（Owner/Admin）；普通成员进入团队只能**使用**（只读）。入口在**团队页内**——侧边栏「团队」→ 某个团队 → 左侧「应用」分区（`/team/:id/apps`）；侧边栏没有一级「应用」菜单。应用以**卡片**展示，卡片右上角「管理」进入应用管理页（`/team/:teamId/app/:appId`）。管理页是**应用工作台**——左侧菜单 `配置/日志/监控`（外部应用另有`访问点`）；`配置` 分区为**左配置、右调试对话**。

## 2. 日常操作（团队页 →「应用」分区）

- **进入**：侧边栏「团队」→ 点某个团队卡片 → 左侧分区菜单选「应用」。默认落在「信息」。
- **列表**：每个应用是一张卡片，含 头像 / 名称 / 类型 / 描述 / 创建时间 / 「外部使用」状态。
- **新建**：右上角「新建应用」→ 应用类型 + 头像（可选）+ 名称（≤ 20 字，团队内唯一）+ 描述（≤ 255 字，可选）+「公开到平台」(`is_public`) 开关。团队已由所在分区确定，无需选择。需团队管理员。
- **管理**：卡片**右上角「管理」**进入该应用的管理页。管理页**没有左侧菜单**，一页左右两栏：**左栏「应用信息」、右栏「Agent 配置」**（流程应用右栏只显示「配置能力尚未开放」）。
- **左栏 应用信息**：改名称、描述、头像与「公开到平台」(`is_public`) 或（外部应用）「需要授权访问」(`is_auth`) 开关，点「保存信息」；**应用类型创建后不可修改**（只读标签）。
- **右栏 对话模型**：单选下拉，选项 = 该团队**有权使用的模型**（与团队「模型网关」分区同一份来源：公开模型 + 已授权本团队的私有模型）。可留空（表示不指定模型）。
- **右栏 插件 / 知识库**：多选勾选该应用**允许使用**的插件与知识库。可选项只来自「该团队有权使用」的范围——插件取自团队插件列表（团队自有 + 系统公开/已授权本团队），知识库取自本团队知识库。
- **右栏 提示词**：填写系统提示词（≤ 4000 字）。
- **右栏 对话开场白**：打开「对话开场白」开关并填写内容（≤ 4000 字），保存后**新会话开始时**先向用户展示这段开场白（以应用身份的 AI 气泡呈现）；关闭开关保留内容但不再展示；开场白仅前端展示，不进入模型上下文，改配对下一个新会话即时生效。
- **保存**：右栏四项共用一份配置与同一个「保存配置」按钮，**一次提交完整配置**；左栏基础信息用「保存信息」单独提交。
- **头像**：新建弹窗与左栏「应用信息」都可上传，支持 png/jpg 等图片且 ≤ 5MB；走存储直传管线，仅接受已完成上传登记的文件。新建时是**先上传拿到 objectKey、随创建请求一次性提交**（此时应用还不存在）；管理页里选中即上传生效。
- **公开到平台（内部应用）**：开关写入 `app.is_public`，卡片底部以 已公开/未公开 展示；**发布后**该应用会出现在任意用户的「应用广场」，平台内用户可进入对话。
- **外部应用**：团队「外部应用」分区创建，卡片底部展示发布状态与「需授权/免授权」(`is_auth`)。外部应用**内部用户看不到**；`is_auth=true` 的应用必须经「应用接入」key 换 token 才能被第三方使用，`is_auth=false` 可匿名换取 token。
- **普通成员（Member）**：分区菜单只有 信息 / 应用 / 知识库；应用分区为只读卡片（无新建按钮、无「管理」入口），直接改 URL 访问管理分区或应用管理页会回落到「信息」。后端同样以 403/404 兜底；成员可读配置（`GET /agent-config`）但保存返回 403。
- **应用广场**：侧边栏「应用广场」列出平台内所有公开且已发布的内部应用，任意登录用户可进入对话（非成员凭公开应用可建会话）。
- **应用接入**：团队「应用接入」分区创建 key 并勾选其可访问的外部应用（只能选本团队外部应用）。创建后弹窗展示 key；列表中 key 默认掩码，**点击即可查看完整 key 或复制**。编辑可改名称/描述/授权应用但**不能改 key**；删除后 key 立即失效。
- **第三方接入调用流程（外部 token，验收见 @EA-S*）**：
  1. **换 token**：`POST /api/external/token`——只传 `accessAppKey` 得**应用 token**（授权 = 接入配置的全部应用）；传 `accessAppKey + appId + externalUserId`（nickname 可选）得**用户 token**（绑定外部用户 id、仅授权该单个应用）；对 `is_auth=false` 的外部应用只传 `appId` 即可匿名换取（生成临时身份）。
  2. **调用外部接口**：以 `Authorization: Bearer <accessToken>` 访问 `/api/external/*`。外部 token **不能**访问 `/api/*` 内部接口，内部用户 token 也**不能**访问 `/api/external/*`（双 audience 隔离）。
  3. **会话与对话**：`POST /api/external/agent/{appId}/session` 建会话（返回 sessionId，需外部**用户** token，应用 token 无用户身份会 403）；对话走 AG-UI SSE `POST /api/external/agent/{appId}/chat`（体为 AG-UI 协议，`threadId` = sessionId）；`GET /api/external/agent/{appId}/session/list` 与 `GET /api/external/session/{sessionId}/messages` 查会话与消息。会话归属按 `external_user.id` 校验，同一外部身份继承会话与消费记录。
  4. **刷新**：`POST /api/external/token/refresh` 传 `refreshToken` 换新 token 对；管理员调整接入授权范围后，**下次刷新即按最新配置生效**（access token 默认 2 小时、refresh 7 天）。
  5. **吊销**：删除应用接入后其 key 与全部 token 刷新即失效；已签发的 access token 在剩余有效期内仍可用。
- **访问点（外部应用工作台 `access` 分区）**：配置面板标题/欢迎语/占位/主题色/位置/按钮文案/头像/尺寸/默认展开/启用；同页展示对话端点地址与 `<script>` 嵌入代码（需授权应用提示填 `data-key`，key 来自团队「应用接入」）。组件脚本由后端托管：宿主页引入 `{server}/embed/moai-widget.js` 即出现悬浮对话。

## 3. 常见问题

| 现象 | 处理 |
|---|---|
| 找不到「应用」入口 | 应用只在团队内：侧边栏「团队」→ 选团队 → 左侧「应用」分区；侧边栏没有一级「应用」菜单 |
| 看不到「新建应用」/「管理」 | 你在该团队不是管理员/所有者（Member 只能使用）；需先由团队 Owner 授予 Admin |
| 看不到「成员管理」「设置」等分区 | 同上，这些管理分区仅团队 Owner/Admin 可见 |
| 新建 409 | 同团队已有同名应用，换名后重试 |
| 保存配置 403 | 非团队管理员；应用是团队产物，仅管理员可管理 |
| 保存配置 400，提示包含无权使用的插件/知识库 | 绑定范围只含「该团队有权使用」的资源；插件需团队自有或系统公开/已授权，知识库需属于本团队。刷新页面取最新可选项后重选 |
| 保存配置 400，提示模型不可用/无权使用 | 模型需启用且渠道启用；私有模型必须已授权本团队（管理员在「模型网关」里授权）。到团队「模型网关」分区确认后再选 |
| 对话模型下拉为空 | 该团队还没有可用模型（无公开模型、也没有授权给本团队的私有模型）；先让管理员在「模型网关」里授权 |
| 已保存的模型在管理页显示为空 | 该模型被停用或取消了对本团队的授权；绑定不会自动清理，重选一个可用模型并保存即可 |
| 提示词保存 400 | 提示词最长 4000 字 |
| 配置了开场白但对话页看不到 | 开场白只在**新会话**开始时展示：确认开关已启用且内容非空后点「新会话」；历史会话按服务端历史渲染，不回放开场白 |
| 流程应用右栏看不到模型/插件/知识库/提示词 | 正常：流程应用的配置能力尚未开放，右栏只显示提示，左栏基础信息可用 |
| 管理页插件/知识库下拉为空 | 该团队还没有可用的插件/知识库，或它们未授权给本团队；先去团队「插件」/「知识库」分区准备 |
| 绑定过的插件/知识库在下拉里是裸 id | 该资源已被删除（绑定为 JSON 文本，不会级联清理）；重新选择并保存即可 |
| 看不到某个团队的应用 | 该团队的应用仅团队成员可见；非成员访问返回 404 |
| 头像上传报错 | 仅支持图片且 ≤ 5MB；objectKey 必须来自存储直传管线 |
| 新建时选了头像但创建失败 | 头像文件已上传但未被应用引用，属孤儿文件（不影响使用）；重新创建时再选一次即可 |
| 内部应用设为「公开到平台」后广场仍看不到 | 需**先发布**（卡片/管理页发布按钮）；平台只列 已发布 且未禁用的公开内部应用 |
| 「外部应用」菜单看不到 | 该分区仅团队 Owner/Admin 可见 |
| 外部应用在内部应用列表里找不到 | 正常：内部/外部应用相互隔离，外部应用只在「外部应用」分区 |
| 外部应用现在能被第三方调用吗 | 能：先在「应用接入」分区创建 key 并授权该外部应用，第三方凭 key 调 `POST /api/external/token` 换 token 后访问 `/api/external/*`；免授权(`is_auth=false`)应用无需 key，直接凭 appId 匿名换取。会话/对话端点用外部用户 token 访问 `/api/external/agent/{appId}/session|chat` |
| 应用接入 key 忘了保存 | 不会丢：列表里 key 默认掩码，点击即可查看完整 key 或复制 |
| 删除应用接入后外部系统报错 | 正常：删除后 key 立即失效；如仍需访问请重建并更新外部系统配置 |
| 应用类型填错 | 类型不可修改，暂需删除重建（删除能力待开放） |
| 调试对话刷新后不见了 | 正常：调试会话仅存 Redis、不落库，刷新即弃用；重新进入会自动创建新的调试会话 |
| 未发布应用能调试吗 | 能：团队管理员在 `配置` 分区右侧可直接调试，无需先发布 |
| 保存信息/发布后调试对话会丢吗 | 不会：刷新为静默加载，调试面板保持挂载 |
| 「访问点」分区是空的 | 本期为占位，能力在后续版本提供 |
| 「监控」数据为空 | 需有真实对话产生用量；调试会话不计入；且聚合数据最多滞后约 1 分钟，稍后再看 |
| 「监控」没有趋势图 | 本期只提供汇总与按模型分布；按日趋势在后续版本提供 |
| 「监控」数字比预期少 | 数据来自每分钟 flush 的聚合表，最多滞后约 1 分钟；且只统计正式会话（不含调试） |
| 调用 `/debug/session` 返回 404 | 非团队成员，或应用不存在；Member 调用返回 403，流程应用返回 400 |
| 「日志」没有任何记录 | 日志只含**正式会话**（不含调试会话）；且必须有用户真实对话过才有记录 |
| 日志里用户显示为「外部用户 #id」 | 该会话归属外部用户（`user_type != normal`），无内部人名，属正常 |
| 日志只看得到部分消息 | 历史按压缩策略留存，日志为**压缩后视图**；被压缩掉的原文不可回溯 |

## 4. 验收流程

1. `dotnet build src/MoAI/MoAI.csproj` → 0 error，`cd src/MoAI && dotnet run`
2. `node local-dev/app-e2e.mjs` → 121/121 PASS（含 AP-43 应用用量监控 汇总/按模型/权限，AP-42 应用对话日志 分页/过滤/权限/消息详情，AP-40 调试会话创建/角色与类型门禁/不落库、AP-45 对话开场白 配置回读/详情下发/权限与校验，及 AP-13e~y 外部应用/应用接入/应用广场；AP-20 需本地库存在可授权的私有模型，脚本用 root 管理员临时授权给 E2E 团队；无 admin 账号时自动跳过绑定用例）
3. `cd ui && CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run syncapi && npm run typecheck && npm run lint && npm run test` 全绿（`syncapi` 需后端运行中）
4. 浏览器：进入某团队 →「应用」新建 Agent/流程应用（可带头像、可开「允许外部使用」）→ 点卡片右上角「管理」→ 在**同一页**左栏改基础信息（保存信息）、右栏选对话模型 + 勾插件/知识库 + 填提示词（保存配置）→ 换头像；再用 Member 账号确认其团队页只有 信息/应用/知识库 且应用卡片只读（无「管理」）

> 建表：`asserts/*.sql` 已随仓库 DDL 清理移除，本模块无需单独执行建表脚本；库表以现状 / 脚手架产物为准。

### 验收记录

| 日期 | 结果 |
|---|---|
| 2026-09-10 | 首版交付：后端 + 前端 + DDL + E2E 脚本；前端全量 vitest **221/221（39 文件）**、新增文件 eslint 绿；后端构建与 E2E 待系统终端复验 |
| 2026-09-11 | 入口修正：应用管理移入团队页「应用」分区，移除侧边栏 `/app`；成员分区收敛为 信息/应用/知识库；前端全量 vitest **223/223（39 文件）**、`eslint src` 0 error |
| 2026-09-11 | 头像与「允许外部使用」：创建/编辑均可设置；后端 `dotnet build MoAI` 0 error、E2E **35/35 PASS**；前端 `npm run syncapi` 后 `tsc` **0 error**、vitest **226/226（39 文件）**、`eslint .` 0 error |
| 2026-09-11 | 列表改**卡片**展示 + 新增**应用管理页**（`/team/:teamId/app/:appId`）：可配置 Agent 应用允许使用的**插件/知识库**（仅限该团队有权使用的资源）与**系统提示词**；后端起 `GET|PUT /api/app/{id}/agent-config`；后端 0 error、E2E **50/50 PASS**、前端 vitest **232/232（40 文件）**、`tsc`/`eslint` 0 error |
| 2026-09-11 | 应用管理页改**单页左右分栏**（去掉左侧菜单与 `:section?` 路由，决策 D21），右栏新增**对话模型**选择（取值 = 团队可用网关模型，服务端复核越权 400，决策 D22）；后端 0 error、E2E **55/55 PASS**、前端 vitest **232/232（40 文件）**、`tsc`/`eslint` 0 error |
| 2026-09-17 | 对话开场白：配置开关 + 内容随 agent-config 保存，应用详情下发（成员可读），聊天页/调试面板新会话展示；后端 0 error、E2E **121/121**、前端 vitest **310/310**、`tsc`/`eslint` 0 error |

## 5. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-10 | 初版，随应用管理模块（团队下应用的基础信息管理）发布；`app_type` 语义修正为 Agent=0/流程=1 |
| 2026-09-11 | 入口修正（D14）：移除侧边栏一级「应用」与 `/app` 页面，改为团队页「应用」分区（`/team/:id/apps`）；新建弹窗去掉团队选择；Member 只能使用（团队页分区收敛为 信息/应用/知识库，管理分区与 URL 直达均不可达） |
| 2026-09-11 | 新建/编辑弹窗支持应用头像与「允许外部使用」开关（`app.enable_foreign`）；新建时头像先直传再随创建请求提交（决策 D15、D16） |
| 2026-09-11 | 应用列表改**卡片网格**、卡片右上角「管理」进独立管理页（决策 D17）；原编辑弹窗并入管理页「基础信息」分区。新增 Agent 应用配置读写：`GET|PUT /api/app/{id}/agent-config`（提示词 / 知识库 / 插件，成员可读、Admin+ 可写、越权绑定 400、仅 Agent 应用可配）（决策 D18~D20） |
| 2026-09-11 | 管理页由「左菜单 + 四分区」改为**单页左右分栏**（左「应用信息」、右「Agent 配置」），路由简化为 `/team/:teamId/app/:appId`；配置项新增**对话模型**（`ai_model.id`，取值 = 团队可用网关模型），`PUT /agent-config` 请求体增 `modelId`（可空）（决策 D21、D22） |
| 2026-09-14 | 应用管理页升级为**应用工作台**：左侧菜单 `配置/日志/监控`（外部应用 + `访问点` 占位），配置分区改**左配置、右调试对话**；新增 **Redis 调试会话**（未发布可调试、不落库、不计用量，`POST /api/app/{id}/debug/session`）；路由改为 `/team/:teamId/app/:appId/:section?`（决策 D27~D30） |
| 2026-09-14 | 工作台「日志」分区交付：应用对话日志（`GET /app/{id}/logs` 分页/过滤 + `/logs/{sessionId}/messages` 消息详情，Admin+）；前端 `AppLogsSection`（DataTable + 详情 Drawer）。监控/访问点仍为占位（决策 D31） |
| 2026-09-14 | 工作台「监控」分区交付：应用用量监控（`GET /app/{id}/usage`，汇总 + 按模型分布，Admin+；数据源 `ai_model_token_audit`，最多滞后约 1 分钟；不含按日趋势）；前端 `AppMonitorSection`（Statistic + DataTable）。访问点仍为占位（决策 D32） |
| 2026-09-17 | Agent 配置新增**对话开场白**（`app_agent_config.opening_statement/-enabled`）：启用开关 + 内容（≤4000 字）随 `PUT /agent-config` 整体保存；`GET /app/{id}` 详情下发（成员可读）；聊天页与调试面板新会话开始时以 AI 气泡展示（决策 D40） |

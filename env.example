# Docker Compose 环境变量配置

# 后端服务地址 - 前端将使用这个地址连接后端
# 在 Docker 环境中，使用服务名作为主机名
BACKEND_URL=http://moaiapi:8080

# 如果需要从外部访问后端，可以使用以下配置
# BACKEND_URL=http://localhost:5000

# 其他可选配置
# NODE_ENV=production
# ASPNETCORE_ENVIRONMENT=Production 
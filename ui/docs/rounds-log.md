| 87 | 知识库最大文件大小默认值 0→50MB | docs/wiki/ | ✅ | 用户反馈「知识库默认最大文件大小为 50mb」：①`SettingDefinitions.WikiMaxFileSizeKey` DefaultValue "0"→"50"，描述同步「默认 50，0 表示不限制（平台硬上限 1GB）」；i18n zh/en `settings.wikiUpload.maxFileSize.desc` 同步；②前端 `Settings.tsx` 解析兜底改为非负整数否则回退 50（显式 0 仍表示不限制）；③WK-11d 由「恢复 0」改「恢复默认 50」断言（upload-limit 返回 50/52428800 且 2MB preupload 200），e2e 复跑 **32/32**（5020 实例，运行后开发库该设置值即为 50）；④vitest **307/307**（Settings 6/6：默认回显 50、改 80 保存提交 80）、typecheck 0；docs/wiki bdd @WK-S26 措辞/tdd 自检同步（2026-09-17） |
| 88 | 流程应用新增 HTTP 请求节点（http） | docs/app-workflow/ | ✅ | 新节点支持定义各类 HTTP 请求：①引擎 `NodeTypes.Http` + `HttpRequestNodeExecutor`——config 含 method/url/timeoutSeconds/params/headers/bodyType(json/form/text)/body/formEntries/auth(bearer/basic/apiKey)/errorCapture/extract(JsonPath 提取)，URL 与各值支持 `{引用}` 插值（URL 严格解析、其余安静解析），输出 `{statusCode, rawResponse, hasError, errorMessage, ...extract}`，网络错误/超时/非 2xx 默认节点失败挂起、开启报错捕获则完成输出 hasError；响应体上限 5MB、超时 1-300s；执行器留 HttpMessageHandler 注入口供单测打桩；`WorkflowValidator` 调 `ValidateDefinition` 校验白名单/必填/JsonPath/插值引用上游；②设计器「集成」分组节点 + `HttpNodeForm.tsx`（方法/地址变量提示追加/超时/Params-Body-Headers 三 Tab/鉴权/报错捕获/字段提取）+ **cURL 导入**（`curl.ts` 词法解析，Bearer/Basic 提升鉴权、k=v 转表单），共享小部件抽 `node-form-widgets.tsx`/`node-form-shared.ts`，sanitizeSettings 放行 http 键、模板 settings 深拷贝；③验证：引擎单测 **50/50**（HttpRequestNodeTests 12 例）、workflow-e2e **70/70**（WF-21a~j 本地桩回环）、ui utils.test **41/41**、typecheck/lint 0 error；bdd @WF-S22~S27、tdd 映射、sdd D29（2026-09-18） |
